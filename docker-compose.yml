services:
  ark:
    build: .
    ports:
      - '${PORT:-8000}:${PORT:-8000}'
    environment:
      # Optional: your partner API base (no trailing slash)
      ARK_BACKEND_URL: ''
      PORT: ${PORT:-8000}
